export const DEFAULT_CONFIG = {
  mode: 'throttle',
  rate: 500,
  handleWidth: false,
  handleHeight: false,
  handleOffset: false,
  updateOnChange: true,
}

export const DEFAULT_VALUES = {
  canUseDOM: false,
  width: 0,
  height: 0,
  offset: {
    x: 0,
    y: 0,
  },
}
